from building import *

cwd = GetCurrentDir()
src = Glob('*.c')
CPPPATH = [cwd + '/../common', cwd]
group = DefineGroup('Drivers', src, depend = ['TSC_USING_CST9217', 'RT_USING_PIN'], CPPPATH = CPPPATH)

Return('group')
